language: java
jdk:
- openjdk8
sudo: false
script: mvn cobertura:cobertura
after_success:
- bash <(curl -s https://codecov.io/bash)
before_install:
- openssl aes-256-cbc -K $encrypted_6dfdcbc02577_key -iv $encrypted_6dfdcbc02577_iv
  -in secret-key.gpg.enc -out secret-key.gpg -d
